//creating variable (value can be changed overtime)
var name = "Ryan";
name = 'Zen'; //overrides to previous same variable
// alert(name);

//creating constant (whose  value cannot be changed)
const name2 = 'Ryan';
name2 = 'Zen'; /** it will show error since u can't override const variable  */
alert(name2);
